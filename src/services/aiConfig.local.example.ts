/** Copy to aiConfig.local.ts and fill keys for local dev. Never commit the local file. */
export const GROQ_API_KEY = '';
/** OCR.space key from https://ocr.space/ocrapi — speeds up full-doc Scan via cloud. */
export const CLOUD_OCR_API_KEY = '';
